import { Component } from '@angular/core';

@Component({
  selector: 'app-admin-sobre',
  standalone: true,
  templateUrl: './admin-sobre.html',
  styleUrl: './admin-sobre.css',
})
export class AdminSobre {
  // TODO: carregar texto da API, editor (textarea ou rich text), salvar
}
